# @drift/mcp

Read-only local MCP server for Drift repo contracts, repo maps, scan status, findings, allowed context, and task preflight packets.

Installers expose the `drift-mcp` binary from `dist/bin.js`.
