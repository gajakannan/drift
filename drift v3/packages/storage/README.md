# @drift/storage

SQLite storage boundary for local-first Drift repo intelligence state.

This package owns migrations and persistence APIs for repos, scans, facts, conventions, findings, baselines, contracts, audit events, and backup manifests.
