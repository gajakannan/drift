# @drift/cli

The Drift command-line interface for local repo scans, deterministic local answers, repo maps, agent preflight, deterministic checks, backups, restores, and governance.

Installers expose the `drift` binary from `dist/main.js`.
